# us-market-review

美股复盘工作区。每次复盘分析在这里进行，结果以日报形式归档到 `reports/`。

## 复盘报告约定（重要）

**每次复盘"运行"结束后，必须把当次复盘报告保存为 `reports/复盘-{日期}.md` 并 commit 到仓库。**

- `{日期}` 使用当天日期，格式 `YYYY-MM-DD`（例如 `reports/复盘-2026-06-10.md`）。
- 若当天文件已存在，则更新该文件（不要再建新文件），保留原有结构。
- 报告内容按 `reports/模板.md` 的结构撰写。
- 不要凭空编造行情数据；数据来源不明时，明确标注"待补充"或说明来源。
- 写完后立即 commit，提交信息形如 `复盘: YYYY-MM-DD`。

### 执行检查清单（每次运行结束前）

1. 把本次复盘分析整理进 `reports/复盘-{今天日期}.md`（沿用模板结构）。
2. `git add reports/复盘-{今天日期}.md`
3. `git commit -m "复盘: {今天日期}"`
4. 如用户要求，再 `git push`。

## 目录结构

```
.
├── CLAUDE.md          # 项目约定（本文件）
└── reports/
    ├── 模板.md         # 复盘报告模板
    └── 复盘-YYYY-MM-DD.md  # 每日复盘报告
```
